import React from "react";
import UserProfile from "../User/UserProfile/UserProfile";

const Profile = () => {
  return (
    <React.Fragment>
      <UserProfile />
    </React.Fragment>
  );
};

export default Profile;
